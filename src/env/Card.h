#pragma once

#include <iostream>
#include <sstream>
#include <string.h>
using std::ostream;
using std::istream;
using std::stringstream;
using std::endl;
using std::string;

enum Suit {
    CLUB = 0;
    C = 0;
    DIAMOND = 1;
    D = 1;
    HEART = 2;
    H = 2;
    SPADE = 3;
    S = 3;
    NOTRUMP = 4;
    NT = 4;
    DOUBLE = 5;
    X = 5;
    PASS = 6;
    P = 6;
}