#pragma once

#include <iostream>
#include <sstream>
#include <string.h>
using std::ostream;
using std::istream;
using std::stringstream;
using std::endl;
using std::string;

enum Doubled {
    NO = 0,
    X = 1,
    XX = 2,
};
